export class Category{
    id: number;
    title: string;
    size: number;

    constructor(id: number, title: string, size: number){
        this.id = id;
        this.title = title;
        this.size = size;
    }
}